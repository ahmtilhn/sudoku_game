name: Responsive non-scroll UI validation

on:
  push:
    branches: [responsive-nonscroll-ui]
    paths:
      - 'tool/responsive_patches/**'
      - 'tool/verify_non_scroll_layouts.py'
      - '.github/workflows/responsive-nonscroll-ui.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: responsive-nonscroll-ui
  cancel-in-progress: false

jobs:
  apply-validate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: responsive-nonscroll-ui
          fetch-depth: 0

      - name: Apply targeted layout patches
        shell: bash
        run: |
          set -euo pipefail
          for patch in tool/responsive_patches/*.py; do
            echo "Applying $patch"
            python3 "$patch"
          done

      - name: Verify non-scroll contract
        run: python3 tool/verify_non_scroll_layouts.py

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Resolve packages
        run: flutter pub get

      - name: Format changed Dart files
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t files < <(git diff --name-only -- '*.dart')
          if (( ${#files[@]} )); then dart format "${files[@]}"; fi
          git diff --check

      - name: Analyze
        run: flutter analyze --no-fatal-infos

      - name: Commit analyzed layout changes
        shell: bash
        run: |
          set -euo pipefail
          git add lib
          if git diff --cached --quiet; then
            echo 'No layout source changes were required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Make offline screens responsive without scrolling'
          git fetch origin responsive-nonscroll-ui
          git rebase origin/responsive-nonscroll-ui
          git push origin HEAD:responsive-nonscroll-ui

      - name: Baseline full test suite diagnostics
        shell: bash
        run: |
          set +e
          timeout --signal=TERM 10m flutter test --reporter expanded --concurrency=1 > /tmp/flutter-tests.log 2>&1
          status=$?
          set -e
          if [ "$status" -ne 0 ]; then
            echo '--- concise failure markers ---'
            grep -n -E 'FAILED|TestFailure|Expected:|Actual:|Some tests failed|EXCEPTION|Error:|TimeoutException|timed out' /tmp/flutter-tests.log | tail -120 || true
            echo '--- last 180 test log lines ---'
            tail -180 /tmp/flutter-tests.log
          else
            echo '--- test tail ---'
            tail -40 /tmp/flutter-tests.log
          fi
          exit "$status"
