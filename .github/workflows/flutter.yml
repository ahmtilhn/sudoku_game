name: Prototype checks

on:
  push:
    branches: [main, 'agent/**']
  pull_request:

jobs:
  validate-prototype:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repository structure and puzzles
        run: python3 tool/validate_prototype.py
