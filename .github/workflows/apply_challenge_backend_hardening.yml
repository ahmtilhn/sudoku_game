name: Apply challenge backend hardening

on:
  push:
    branches:
      - agent/challenge-system-hardening
    paths:
      - tool/apply_challenge_backend_hardening.py
      - .github/workflows/apply_challenge_backend_hardening.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/challenge-system-hardening
          fetch-depth: 0
      - name: Apply backend hardening
        run: python tool/apply_challenge_backend_hardening.py
      - name: Remove temporary transformer
        run: |
          rm tool/apply_challenge_backend_hardening.py
          rm .github/workflows/apply_challenge_backend_hardening.yml
      - name: Commit generated backend changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Harden challenge backend lifecycle"
          git push origin HEAD:agent/challenge-system-hardening
