name: Direct result dialog patch

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/tmp-result-dialog-direct.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Replace result carrier
        shell: python
        run: |
          from pathlib import Path

          path = Path('lib/features/duel/online_duel_screen.dart')
          source = path.read_text()
          old = """      final action = await showModalBottomSheet<String>(
        context: context,
        isDismissible: false,
        enableDrag: false,
        isScrollControlled: true,
        useSafeArea: true,
        backgroundColor: Colors.transparent,
        barrierColor: Colors.black.withValues(alpha: .56),
        constraints: const BoxConstraints(maxWidth: 460),
        builder: (sheetContext) => FractionallySizedBox(
          heightFactor: MediaQuery.sizeOf(sheetContext).height < 720
              ? .98
              : .94,
          child: _OnlineResultSheet(snapshot: snapshot),
        ),
      );
"""
          new = """      final action = await showGeneralDialog<String>(
        context: context,
        barrierDismissible: false,
        barrierLabel: 'Duel result',
        barrierColor: Colors.black.withValues(alpha: .68),
        transitionDuration: const Duration(milliseconds: 220),
        transitionBuilder: (dialogContext, animation, secondaryAnimation, child) {
          final curved = CurvedAnimation(
            parent: animation,
            curve: Curves.easeOutCubic,
            reverseCurve: Curves.easeInCubic,
          );
          return FadeTransition(
            opacity: curved,
            child: ScaleTransition(
              scale: Tween<double>(begin: .97, end: 1).animate(curved),
              child: child,
            ),
          );
        },
        pageBuilder: (dialogContext, animation, secondaryAnimation) {
          final media = MediaQuery.of(dialogContext);
          final availableHeight =
              media.size.height - media.padding.top - media.padding.bottom - 16;
          return SafeArea(
            minimum: const EdgeInsets.all(8),
            child: Material(
              type: MaterialType.transparency,
              child: Center(
                child: ConstrainedBox(
                  constraints: const BoxConstraints(maxWidth: 460),
                  child: SizedBox(
                    width: double.infinity,
                    height: availableHeight.clamp(0.0, 900.0).toDouble(),
                    child: _OnlineResultSheet(snapshot: snapshot),
                  ),
                ),
              ),
            ),
          );
        },
      );
"""
          if old not in source:
              raise SystemExit('result carrier block not found')
          path.write_text(source.replace(old, new, 1))

      - name: Verify patch
        shell: python
        run: |
          from pathlib import Path
          source = Path('lib/features/duel/online_duel_screen.dart').read_text()
          assert 'showGeneralDialog<String>' in source
          assert 'showModalBottomSheet<String>' not in source
          assert 'availableHeight' in source

      - name: Commit patch
        shell: bash
        run: |
          set -euo pipefail
          git add lib/features/duel/online_duel_screen.dart
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'Open duel results in stable dialog'
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
